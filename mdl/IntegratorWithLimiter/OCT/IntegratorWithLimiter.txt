IntegratorWithLimiter Model OCT Notes

OCT event indicator support must be enabled for QSS simulation since
the model has a zero-crossing if clause.

The discrete y variable is made an output so that it appears in the
reverse dependency of the event indicator.
