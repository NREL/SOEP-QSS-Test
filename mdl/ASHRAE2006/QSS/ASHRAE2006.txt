ASHRAE2006 Model QSS Notes

As of the 2020/11 OCT update the event indicator dependency information
is believed to be incorrect/incomplete causing the QSS simulation
to give incorrect results and very slow run times.

The QSS run gives warnings about initial values from the xml not
equaling the initial values from the fmi2GetContinuousStates call.

The QSS run gives this warning: Number of FMU-ME event indicators (68)
is not equal to the number of zero-crossing variables found (56)


Investigation Plan:
- Compare shorter runs with full QSS diagnostics
  - Look at variables dominating requantizations
  - Find first point of divergence and identify cause (missed ZC?)
    - Get ZC event times out of PyFMI by running with and without --soo arg to run_PyFMI.py
- Try LIQSS runs to see if stiffness is a factor
