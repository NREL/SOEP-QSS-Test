Case600FF Model Dymola Notes

The FMU run with PyFMI (using run_fmu.py) generates many .out files so these
are not included in the repository currently. Options for setting a reduced
sampling rate and subsetting the outputs will be looked at.
