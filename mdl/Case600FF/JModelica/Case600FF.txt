Case600FF Model JModelica Notes

The current FMU depends on libgfortran.so.3 so won't work on systems with
GCC 7+ that uses later versions of libgfortran.

The compile_fmu.Buildings.JModelica.py script gives errors for this model.
