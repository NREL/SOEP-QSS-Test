#!/bin/bash
# Set environment for using these scripts
# Customize for the directory of the Buildings library
export MODELICAPATH=/opt/JModelica/ThirdParty/MSL:/opt/modelica-buildings
#export MODELICA_BUILDINGS_LIB=/opt/modelica-buildings
export PATH=$PATH:$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )
